// src/lib/admins.js
// Sin dependencias — se importa desde middleware.js (edge runtime) y desde
// src/lib/auth-admin.js (route handlers), así ninguno arrastra al otro.

export const ADMINS = ["jmrmarroquineria@gmail.com"];
